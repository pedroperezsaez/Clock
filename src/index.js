const clock = document.getElementById('navClock');
const alarm = document.getElementById('navAlarm');
const stopwatch = document.getElementById('navStopwatch');
const timer = document.getElementById('navTimer');
function clickReloj(){
    if(clock){
    clock.addEventListener('click', function(){
        const seleccionadoantes =document.querySelector('.seleccionado')
    } );
}}
